import { createContext, useContext, useEffect, useState, type ReactNode } from "react";
const URL="https://gwfqapwfnvklgykuhskn.supabase.co"; const KEY="sb_publishable_dhgPCN6uB4gbYftY9l_pVA_RaydtKfF";
type M={module_key:string;metadata:Record<string,unknown>;enabled:boolean}; type S={modules:M[];maintenance:boolean};
const C=createContext<S>({modules:[],maintenance:false});
async function load(siteKey:string):Promise<S>{const h={apikey:KEY,Authorization:"Bearer "+KEY},k=encodeURIComponent(siteKey);const[a,b]=await Promise.all([fetch(URL+"/rest/v1/site_modules?site_key=eq."+k+"&select=module_key,metadata,enabled",{headers:h}),fetch(URL+"/rest/v1/site_control_configs?site_key=eq."+k+"&select=config,enabled&limit=1",{headers:h})]);const modules=a.ok?await a.json():[],l=b.ok?await b.json():[];return{modules,maintenance:Boolean(l?.[0]?.enabled&&l?.[0]?.config?.site?.maintenanceMode)}}
export function SiteControlProvider({siteKey,children}:{siteKey:string;children:ReactNode}){const[state,setState]=useState<S>({modules:[],maintenance:false});useEffect(()=>{let on=true;const f=()=>load(siteKey).then(v=>on&&setState(v)).catch(()=>{});f();const t=window.setInterval(f,30000);return()=>{on=false;clearInterval(t)}},[siteKey]);return <C.Provider value={state}>{state.maintenance?<div style={{minHeight:"100vh",display:"grid",placeItems:"center",fontFamily:"system-ui"}}><div style={{textAlign:"center"}}><h1>Site Maintenance</h1><p>We'll be back shortly.</p></div></div>:<>{children}<Disabled/></>}</C.Provider>}
function Disabled(){const s=useContext(C);const path=typeof window==="undefined"?"":window.location.pathname;const m=s.modules.find(x=>x.metadata?.route===path);if(!m||m.enabled)return null;return <div style={{position:"fixed",inset:0,zIndex:99999,background:"white",display:"grid",placeItems:"center",fontFamily:"system-ui",padding:24}}><div style={{textAlign:"center"}}><h1>Module Unavailable</h1><p>This section is currently unavailable.</p></div></div>}
export function useSiteControl(){return useContext(C)}
